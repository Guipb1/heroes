# Copie para ".env.test" (não é commitado).
# Aponta para um banco SEPARADO do banco de desenvolvimento, para os testes
# poderem apagar/recriar dados livremente sem afetar o que você está usando
# manualmente. Ver README para o comando que cria esse banco no Docker.

DATABASE_URL="mysql://root:rootpass@localhost:3306/hero_factory_test"
